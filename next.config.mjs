/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/testing-next",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  export default nextConfig;
